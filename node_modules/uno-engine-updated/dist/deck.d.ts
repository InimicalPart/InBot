import { Card } from './card';
export declare class Deck {
    private originalDraw;
    private shuffle;
    private discarded;
    readonly cards: Card[];
    readonly discardedPile: Card[];
    readonly length: number;
    discard(card: Card): void;
    reshuffle(): void;
    draw(num?: number): Card[];
}
